{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron text-center mb-0">
        <h1>Camping *** l'Espadrille Volante</h1>
    </div>
    <div class="row">
        {{ include('home/_menu.html.twig') }}
    </div>

    <div class="container">
        <div>
            <h2 class="m-5">Piscine</h2>
            <div class="mb-5 d-flex justify-content-around">
                <img src="{{ vich_uploader_asset( images[1], 'imageFilePresentation' ) }}" alt="piscine">
                <img src="{{ vich_uploader_asset( images[2], 'imageFilePresentation' ) }}" alt="piscine">
                <img src="{{ vich_uploader_asset( images[3], 'imageFilePresentation' ) }}" alt="piscine">
                <img src="{{ vich_uploader_asset( images[4], 'imageFilePresentation' ) }}" alt="piscine">
            </div>
            <p>
                {{ presentation.infoPool }}
            </p>
            <h2 class="mb-3 ml-5 mt-5">Tarifs</h2>
            <ul class="list-unstyled">
                {% for price_pool in prices_pool %}
                    <li>
                        {{ price_pool.label }} : {{ price_pool.price / 100 }} € par jour
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}