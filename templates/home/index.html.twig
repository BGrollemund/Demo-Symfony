{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron text-center mb-0">
        <h1>Camping *** l'Espadrille Volante</h1>
    </div>
    <div>
        <img src="{{ vich_uploader_asset( image, 'imageFilePresentation' ) }}" alt="notre camping" class="min-vw-100">
    </div>
    <div class="row">
        {{ include('home/_menu.html.twig') }}
    </div>

    <div class="container">
        <div class="mb-5">
            <h2 class="mb-5">{{ presentation.title }}</h2>
            <p>
                {{ presentation.description }}
            </p>
        </div>
    </div>
{% endblock %}
