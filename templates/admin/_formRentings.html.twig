{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">{{ form_row(form.label) }}</div>
    <div class="col-md-6">{{ form_row(form.location) }}</div>
</div>
<div class="row">
    <div class="col-md-6">{{ form_row(form.renting_type) }}</div>
</div>
<div class="row">
    <div class="col-md-4">{{ form_row(form.renter_type) }}</div>
    <div class="col-md-8">{{ form_row(form.users) }}</div>
</div>
<button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}