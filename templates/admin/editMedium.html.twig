{% extends 'base.html.twig' %}

{% block title %}Admin - Camping *** l'Espadrille Volante{% endblock %}

{% block body %}
    <div class="jumbotron text-center mb-0">
        <span class="float-right">
                <a href="{{ path('logout') }}" class="btn btn-info">Déconnexion</a>
        </span>
        <h1>Camping *** l'Espadrille Volante</h1>
    </div>
    <div class="row">
        {{ include('admin/_menu.html.twig') }}
    </div>

    <div class="container">
        <div class="mb-5">
            <h2 class="mb-5">Editer la photo de la location</h2>
            <div class="col-8">
                <img src="{{ vich_uploader_asset( medium, 'imageFile' ) }}" alt="">
            </div>
            <div>
                {{ include('admin/_formMedia.html.twig', {form : form}) }}
            </div>
        </div>
    </div>

{% endblock %}